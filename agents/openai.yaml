interface:
  display_name: "TheWorkshop"
  short_description: "Run mixed-work projects with auditable plans and local orchestration."
  default_prompt: "Use $theworkshop to turn this request into a Project -> Workstreams -> Jobs plan, execute it with TheWorkshop gates, and explicitly use native Codex subagents for independent bounded jobs when parallel work is justified."

policy:
  allow_implicit_invocation: true
