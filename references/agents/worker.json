{
  "schema": "theworkshop.agentprofile.v1",
  "name": "theworkshop_worker",
  "legacy_aliases": ["worker", "default", "implementer", "builder"],
  "runtime_agent_name": "theworkshop_worker",
  "builtin_fallback_agent_type": "worker",
  "agent_type": "worker",
  "description": "Primary implementation profile for execution jobs with explicit output contracts.",
  "default_for": {
    "stakes": ["normal", "high", "critical"],
    "orchestration_mode": ["execute", "implement", "delegate", "subagent"]
  },
  "execution": {
    "default_dispatch_budget": 1,
    "default_retry_limit": 2,
    "preferred_parallel_group_size": 3,
    "notes": "Use for concrete job execution; enforce truth/reward gates before completion."
  }
}
